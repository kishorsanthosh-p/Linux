pipeline {
    agent {
  stages {
    stage('Build') {
      steps {
        // Perform build steps (e.g., installing dependencies) for your Python application
        sh 'npm install'
        sh 'ls'
      }
      stage('Build') { 
            steps {
                sh 'npm install' 
            }
        }
    }
